<!DOCTYPE html>
<html>
<head>
    <style>
        table {
            font-family: arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }

        td, th {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #577bbd;
        }
    </style>
</head>
<body>

<center> <h1>List of Tasks</h1></center>
<table>
    <tr>
        <th style="width:20%">Date</th>
        <th style="width:10%">Ticket</th>
        <th style="width:70%">Tsk Description</th>
    </tr>

    {% for task in tasks %}
        <tr>
            <td>{{ task.startDate|date('Y-m-d') }} - {{ task.endDate|date('Y-m-d') }}</td>
            <td>{{ task.ticket.name }}</td>
            <td>{{ task.description }}</td>
        </tr>
    {% endfor %}
</table>

</body>
</html>
